# Pinned for reproducible builds. Bare package names meant every rebuild could
# pull a new major version — or a compromised release — with no review.
# Bump deliberately, and re-run the test suite (pytest) afterwards.
flask==3.1.3
gunicorn==23.0.0
python-dotenv==1.2.2
pymupdf==1.27.1
Pillow==12.1.1
landingai-ade==1.8.0
supabase==2.28.2
